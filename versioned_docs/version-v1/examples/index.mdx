---
title: "Examples"
description: "Example WebAssembly components and capability providers for wasmCloud in Rust, Go, and TypeScript."
proficiency: Intermediate
# `about` (single primary topic) and `mentions` (secondary topics) reference
# entity slugs in src/data/entities.json. At build time these expand into
# schema.org JSON-LD nodes — emitted as @id refs on the page's Article-family
# node plus inlined Thing entries in the same @graph payload. Slugs are
# case-sensitive. Transcript pages inherit these refs from their parent
# landing page via src/data/transcript-inheritance.json (regenerated by
# scripts/generate-transcript-inheritance.mjs / the prebuild hook), so
# don't repeat the block in transcript frontmatter.
about: wasmCloud
mentions:
  - Go
  - TypeScript
  - Rust
  - WebAssembly
platforms: [wasmCloud]
---

This section includes examples of [WebAssembly components](/docs/v1/concepts/components/) and [capability providers](/docs/v1/concepts/providers/) for wasmCloud. Examples are organized by programming language, and then by type: component or provider.

:::warning[Under construction]
We are in the process of automating the propagation of examples from [wasmCloud repositories on GitHub](https://github.com/wasmCloud/) to this documentation. Examples in **Go** currently propagate from the [`wasmCloud/go` repo](https://github.com/wasmCloud/go).

You can find additional examples in other languages such as Rust, TypeScript, and more in the [`wasmCloud`](https://github.com/wasmCloud/wasmCloud/tree/main/examples), [`wasmCloud/typescript`](https://github.com/wasmCloud/typescript), and [`wasmCloud/contrib`](https://github.com/wasmcloud/contrib) repositories.
:::